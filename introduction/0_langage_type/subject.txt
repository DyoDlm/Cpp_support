Hellooo

Ecrire les fonctions suivantes prototypees telles que : 


size_t	ft_strlen(const char *s);
-->	renvoyer la longueur de la chaine

void	ft_strToUpper(const char *s);
-->	afficher la string en minuscules
 
void	ft_strToLower(const char *s);
-->	afficher la string en majuscules 

bool	isHot(const char *s);
-->	si s est "seite" alors return true, sinon false 

void	*ft_memcpy(void *src, void *dst, size_t dst_size);
-->	Copy src to dst and return pointer of dst

Tu pourras tester avec test.sh tel que :
	./test.sh strlen
	./test.sh strToUpper
	ou
	./test.sh all
