#include "easyfind.hpp"

EasyFind::EasyFind()
{
    return ;
}

EasyFind::EasyFind(const EasyFind &f)
{
    (void)f;
}

EasyFind::~EasyFind()
{
    return ;
}

template <typename T>
int   EasyFind::easyfind(T in, int find)
{
    int param;

    for (std::T<int>::iterator i = T.begin(); i != T.end(); i++)
        if (*i == i)
            return (*i);
    return 0;
}
